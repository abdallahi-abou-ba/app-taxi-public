# Used only by `npm run dev` (Vite dev server) to reach a locally running
# backend on a different port. Leave unset (or empty) for production builds -
# the built app is served same-origin from the backend at /admin, so a blank
# base URL means plain relative /api/... requests, no CORS needed.
VITE_API_BASE_URL=http://localhost:3000
